import { Box, Typography } from "@mui/material";

export const MessingAroundTab = () => {
    return (
        <Box>
            <Typography>
                Messing around
            </Typography>
        </Box>
    );
}